<section class="section-favorites {{section-class}}">
    <div class="section-favorites__container __container">
        <h1 class="section-favorites__title title-lg font-semibold">Избранное</h1>
        <div class="section-favorites__wrapper">
            {{#each favorites-context.list}}
                {{> card }}
            {{/each}}
        </div>
    </div>
</section>


