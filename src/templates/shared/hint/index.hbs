<div class="hint" data-hint-list>
    <ul class="hint__list list-reset" >
        {{#each items}}
            <li class="hint__item">
                <a href="#" class="hint__link">{{this.value}}</a>
            </li>
        {{/each}}
    </ul>
</div>
